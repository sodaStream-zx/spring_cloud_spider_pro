#---
spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: config-server
        enabled: true
      profile: pro
      label: master
  application:
    name: zuul-server
eureka:
  client:
    service-url:
      defaultZone: http://${spring.cloud.client.ipAddress}:3001/eureka/
