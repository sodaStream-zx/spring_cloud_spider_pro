#---
spring:
  application:
    name: eureka-server
  cloud:
    config:
      fail-fast: true
     # discovery:
       # service-id: config-server
      #  enabled: true
      profile: on
      label: master
      uri: http://${spring.cloud.client.ipAddress}:10000/
#eureka:
#  client:
#    service-url:
#      defaultZone: http://${spring.cloud.client.ipAddress}:3001/eureka/
